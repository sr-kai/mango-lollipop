---
name: Iterate
description: Modify the lifecycle matrix conversationally (add, remove, change messages)
---

Read and follow the instructions in `skills/iterate/SKILL.md` exactly.

Look for `mango-lollipop.json`, `matrix.json`, `analysis.json`, and `messages/` in the current directory or `output/*/` subdirectories.

$ARGUMENTS
